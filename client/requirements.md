## Packages
recharts | For data visualization (Pie, Bar charts)
framer-motion | For smooth transitions and animations
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes

## Notes
The dashboard requires a specific SVG map of India with highway routes.
Print styles are critical for the 'Print Dashboard' feature.
Data comes from a consolidated /api/dashboard endpoint which needs to be transformed on the client side to match the nested structure expected by the UI.
